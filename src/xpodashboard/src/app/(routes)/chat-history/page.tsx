'use client';

import Nav from '@/components/core/Nav';
import ChatHistory from '@/components/chatgeschiedenis/ChatHistory';

export default function ChatHistoryPage() {
  return (
    <Nav>
      <ChatHistory />
    </Nav>
  );
} 